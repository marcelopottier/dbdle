# Copy to .env and fill in the values
OPENAI_API_KEY=sk-proj-...

# URL the frontend uses to reach the backend.
# In Docker: leave empty — nginx proxies /game, /assets, /proxy internally.
# In dev (pnpm dev): set to http://localhost:8081
VITE_API_URL=
